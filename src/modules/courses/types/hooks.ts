/**
 * Courses模块Hooks类型定义
 * Re-export hooks中定义的类型,便于统一导入
 */

export type { UseProjectMatrixResult, ProjectMatrixData, KsaItem } from "../hooks/use-project-matrix"
export type { UseTaskObjectivesResult } from "../hooks/use-task-objectives"
export type { UseKsaManagementResult } from "../hooks/use-ksa-management"
